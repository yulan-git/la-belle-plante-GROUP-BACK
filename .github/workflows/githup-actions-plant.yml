name: CI-CD
on: 
  push:
    branches: [ master ]

  workflow_dispatch:

jobs:

  deploy: 
    runs-on: ubuntu-latest

    steps: 
    - name: Deploy App
      uses: actions/checkout@v2
    
    - uses: akhileshns/heroku-deploy@v3.12.12 # This is the action
      with:
        heroku_api_key: ${{ secrets.LA_BELLE_PLANTE_BACK }}
        heroku_app_name: "labelleplanteback"
        heroku_email: "gaellecoue@gmail.com"
